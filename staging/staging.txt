const content = document.getElementById("codex-content");
content.className = "";
content.scrollTop = 0;

......... 

.codex-scroll-fade {
  mask-image:
    linear-gradient(
      to bottom,
      transparent 0,
      black 12px,
      black calc(100% - 10px),
      transparent 100%
    );

  -webkit-mask-image:
    linear-gradient(
      to bottom,
      transparent 0,
      black 12px,
      black calc(100% - 10px),
      transparent 100%
    );
}